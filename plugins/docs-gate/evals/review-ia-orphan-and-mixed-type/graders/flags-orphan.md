---
type: llm
---

resources/docs/orphan.md exists on disk but is never referenced in resources/docs/sidebars.json.

PASS if the response identifies orphan.md (or "the migration notes page" / similar description of
it) as unlinked, unreferenced, or an orphan.
FAIL if the response doesn't mention this page as an orphan/unlinked page at all.

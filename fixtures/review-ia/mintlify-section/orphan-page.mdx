<!-- SYNTHETIC FIXTURE: written for docs-gate testing only.
     Seeded defect: this file exists on disk but is never referenced in
     docs.json's navigation, so it should surface as an orphan candidate. -->

# Orphaned migration notes

This page exists but was never added to the nav.
